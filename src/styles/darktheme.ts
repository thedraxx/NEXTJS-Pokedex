import { createTheme } from "@nextui-org/react";

export const darkTheme = createTheme({
  type: "dark",
  theme: {
    colors: {
      primary: "#ff0000",
      secondary: "#00ff00",
      success: "#00ff00",
      error: "#ff0000",
    },
  },
});
